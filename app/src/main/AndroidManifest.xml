<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.betaac">

    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BetaaC">
        <activity android:name=".chat.GroupChatActivity"></activity>
        <activity android:name=".ContactActivity" />
        <activity android:name=".chat.SpecificChatActivity" />
        <activity android:name=".user.UpdateProfileActivity" />
        <activity android:name=".user.ProfileActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".user.SetProfileActivity" />
        <activity android:name=".user.OtpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>